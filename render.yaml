services:
  - type: web
    name: product-caption-api
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port 10000
    envVars:
      - key: HUGGINGFACE_API_TOKEN
        sync: false
